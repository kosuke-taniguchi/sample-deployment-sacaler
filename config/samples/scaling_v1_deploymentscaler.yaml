apiVersion: scaling.example.com/v1
kind: DeploymentScaler
metadata:
  labels:
    app.kubernetes.io/name: sample-deployment-scaler
    app.kubernetes.io/managed-by: kustomize
  name: deploymentscaler-sample
spec:
  target:
    name: server
    namespace: default  # 明示的に指定(省略時はCRと同じ)
  replicas: 3
